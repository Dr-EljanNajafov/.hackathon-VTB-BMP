server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vtb?useSSL=false&amp;serverTimezone=UTC
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
springdoc:
  swagger-ui:
    path: "/docs"